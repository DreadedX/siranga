apiVersion: v1
kind: Service
metadata:
  name: http
spec:
  ports:
    - name: "3000"
      port: 3000
      targetPort: 3000
  selector:
    app: siranga
---
apiVersion: v1
kind: Service
metadata:
  name: ssh
  annotations:
    external-dns.alpha.kubernetes.io/hostname: tunnel.svc.${domain}
spec:
  type: LoadBalancer
  ports:
    - name: "2222"
      port: 22
      targetPort: 2222
  selector:
    app: siranga
